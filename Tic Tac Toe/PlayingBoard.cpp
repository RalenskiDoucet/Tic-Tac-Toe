#include "PlayingBoard.h"

PlayingBoard::PlayingBoard()
{
 PlayingBoard Grid;
 Grid  mRows&mCols;
}
PlayingBoard PlayingBoard::getnumOfRows(int rows)
{
	mRows = rows;
	for (Grid; getnumOfRows; rows = 3)
	{
		
	}
	return mRows;
}

PlayingBoard PlayingBoard::getnumOfCols(int cols)
{
	mCols = cols;
	for (Grid;getnumOfCols;cols=3)

	return PlayingBoard();
}



PlayingBoard PlayingBoard::getGrid(const int Grid)
{
	int rows; int cols;
	rows = mRows; cols = mCols;
	for (Grid;getGrid;)
	{ }

	return PlayingBoard();
}
